{
  "name": "upp-landing-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check netlify/functions/waitlist.js && node --check netlify/functions/waitlist-count.js && node --test",
    "build": "npm run check && node scripts/build.mjs"
  }
}
